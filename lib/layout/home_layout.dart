import 'package:flutter/material.dart';

class HomeLayout extends StatelessWidget {
  static const String routName = 'home layout';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
